<%= form_with(model: position, local: true) do |form| %>
  <% if position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :portfolio %>
    <%= form.text_field :portfolio, id: :position_portfolio %>
  </div>

  <div class="field">
    <%= form.label :security %>
    <%= form.text_field :security, id: :position_security %>
  </div>

  <div class="field">
    <%= form.label :shares %>
    <%= form.text_field :shares, id: :position_shares %>
  </div>

  <div class="field">
    <%= form.label :asof %>
    <%= form.datetime_select :asof, id: :position_asof %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
